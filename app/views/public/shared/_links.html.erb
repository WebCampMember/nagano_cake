<%- if controller_name != 'sessions' %>
  <h4>既に登録済みの方</h4>
  <p><u><%= link_to "こちら", new_session_path(resource_name) %></u>　からログインしてください。</p>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <h4>会員登録がお済みでない方</h4>
  <p><u><%= link_to "こちら", new_registration_path(resource_name) %></u>　から新規登録を行ってください。</p>
<% end %>
