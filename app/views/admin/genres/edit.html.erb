<h1>ジャンル編集</h1>
  <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
    <label for="GenretName">ジャンル名</label>
    <%= f.text_field :genre_name, autofocus: true, id:"GenreName", placeholder:"例：麺類" %>
    <%= f.submit "変更を保存", class:"btn btn-secondary" %>
  <% end %>